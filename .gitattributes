# Normalise to LF in the repository. A shell script or Dockerfile checked out
# with CRLF fails on Linux with "bad interpreter" / unexpected token errors,
# which is a miserable first experience for anyone cloning this on the host that
# actually has the hardware.
* text=auto eol=lf
*.png binary
*.jpg binary
